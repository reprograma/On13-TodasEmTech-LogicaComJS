
    Tarefa 01

    //1. Crie duas variáveis para armazenar o nome de usuário e a senha dentro do script;
    
    const nomeDeUsuario = "nome";
      console.log(nomeDeUsuario);
    
      const senha = "senha";
      console.log(senha);

    //2. Use um `prompt` para pedir ao usuário seu nome e armazene o resultado numa variável;
    
    var nomeUsuario = prompt("Qual é o seu nome?");
    let nomeUsuario = "Anna";
 
    
    //3. Use outro `prompt` para pedir ao usuário sua senha e a armazene em outra variável;
    
    var senhaUsuario = prompt("Qual é a sua senha?");
    let senhaUsuario = "00000000";

    /*4. Verifique se o nome de usuário e a senha estão corretos:
      a. caso afirmativo, cumprimente o usuário com um `Bom dia!` por meio de um `alert`;
      b. caso as credenciais estejam erradas, informe o usuário
      (aqui não precisamos pedir as credenciais novamente; podemos finalizar normalmente)
 */
      usuario = "Anna";
      let senha = 00000000
      let nomeDeUsuario = (prompt("Digite o nome do usuário"));
      let senhaDeUsuario = (prompt("Digite a sua senha"));
      
      if (nomeDeUsuario == usuario) && (senhaDeUsuario = senha) {
        alert('Bom dia');
      } else {
        alert('Os dados do usuário estão incorretos');
      }
    
  










      
      
        
