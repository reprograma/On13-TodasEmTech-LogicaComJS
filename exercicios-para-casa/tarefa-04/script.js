/**
    Tarefa 04

    1. Receba o preço de cada um dos 10 itens;
    2. Mostre na tela a soma de todos os preços.
 */

    let preco = [];

    let somaPreco = 0;

    for (let i=0; i <10; i++) {
        precos.push(Number(prompt("Digite o preço")));
    }

    for(let item = 0; item < precos.length; item++) {
        somaPrecos +- precos [item];
    }

    alert(somaPreco);
