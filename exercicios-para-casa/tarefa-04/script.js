/**
    Tarefa 04

    1. Receba o preço de cada um dos 10 itens;
    2. Mostre na tela a soma de todos os preços.
 */

    let preco = 0;
    let soma = 0;

    while (preco < 10){
        prompt("Insira o preço" + preco);
        preco ++;
    }
    soma +- preco;
    ++soma;
    alert('a soma é &(soma');

    let i = 0;
    let precoItens = 0;
    let soma = 0;

    while (i < 10) {
        precoItens = parseFloat(prompt('Insira o valor do item'));
        soma += precoItens;
        i ++;
    }

    alert('A soma dos itens será: R$ ${soma}');

    
